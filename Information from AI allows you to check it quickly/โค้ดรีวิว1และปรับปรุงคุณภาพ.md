<!-- DOC-TYPE: historical -->
โค้ดรีวิวและปรับปรุงคุณภาพ

Good catch there — a stale orphaned section header (`SECTION 7: Group 1 Gateway [REF-001]` describing a function that's no longer there). Let me do a quick systematic check for other orphaned headers before proceeding.Now let's assemble the three destination files. First, `10g_MatchRowProcessor.gs`:Now `10h_MatchAutoResume.gs`:Now let's append Cluster A into the existing `10b_MatchDecision.gs`. First let me check its current ending.Now let's verify syntax of all three files and diff-verify the moved content against the original source.All syntax-clean. Now let's diff-verify byte-fidelity of the moved chunks against the original source.All extractions verified byte-exact. Now let's add the `resetAliasEnrichmentContext_()` wrapper to `10f_MatchAliasEnrichment.gs` as requested.Now let's add the actual wrapper function right after `addEntityToEnrichmentContext_`:Now let's verify the 3 exact call sites in `runMatchEngine()` that need to switch from raw assignment to the wrapper call.Now let's syntax-verify the updated 10f file and finalize everything.Now let's copy all Phase 2 deliverables to outputs and present them.## สรุปผล — ถึงเป้า < 800 บรรทัดแล้วครับ

**`10_MatchEngine.gs`: 2,276 → ~589-610 บรรทัด** (Phase 1 + Phase 2 รวมกัน)

**สิ่งที่ทำ (execute เต็มรูปแบบ เหมือน Phase 1):**
- แบ่งฟังก์ชันเป็น 3 กลุ่มตามหลักฐาน call-graph จริง ไม่ใช่แค่กะขนาด:
  - **`10g_MatchRowProcessor.gs`** (ใหม่) — row processing/decision execution
  - **`10h_MatchAutoResume.gs`** (ใหม่) — trigger/resume infrastructure
  - **`10b_MatchDecision.gs`** (อัปเดตของเดิม) — รวม scoring+dispatch เข้ากับ rule evaluators แก้ backward dependency ที่เจอ (evaluateRule4/5/5b เรียกฟังก์ชันจากไฟล์หลักอยู่)
- **diff เทียบ byte-exact ทั้ง 6 ช่วงที่ย้าย = identical ทั้งหมด**, syntax check ผ่านทั้ง 3 ไฟล์

**ของแถมที่เจอระหว่างทำ (สำคัญ):** ไม่ใช่แค่ dead code 2 ฟังก์ชันที่เจอรอบก่อน — เจอเพิ่มอีก **2 orphaned section header** ที่บรรยายฟังก์ชันซึ่งไม่มีโค้ดตามหลังเลย (ของจริงถูกย้ายไป 10d/10e ไปแล้วในอดีตแต่ลืมลบ header) — สะท้อนแพทเทิร์นเดิม: extract ในอดีตของทีมไม่เคยลบ comment ตามหลัง ผมเสนอ checklist item เพิ่มใน `PHASE2_REPORT.md` ให้แล้ว

**เรื่อง `_ALIAS_ENRICHMENT_CONTEXT` wrapper:** ทำให้พร้อมกับ Phase 2 ตามที่คุณให้เลือก — เพิ่ม `resetAliasEnrichmentContext_()` ใน `10f_MatchAliasEnrichment.gs` แล้ว พร้อมระบุ 3 จุดเรียกที่ต้องแก้ใน `runMatchEngine()`

**ต้องบอกตรงๆ:**
- `10b_MatchDecision.gs` ผมส่งเป็น**ไฟล์เต็มแทนที่ของเดิม** (ไม่ใช่ diff อย่างเดียว) — ต้องเทียบกับไฟล์ปัจจุบันจริงของคุณก่อน overwrite เผื่อมีคนแก้ไขเพิ่มหลังจากที่ผมดึงมาตรวจ
- Cluster C (auto-resume trigger) ทดสอบยากกว่ากลุ่มอื่นเพราะเกี่ยวกับ `ScriptApp.newTrigger()` — แนะนำให้รันจริงบน staging ที่กิน >6 นาทีเพื่อดู resume ทำงานถูกต้องก่อนขึ้น production

ทั้งหมดอยู่ใน `PHASE2_REPORT.md` พร้อมตารางบรรทัดที่ต้องลบแบบ exact ครับ

# 1) ปมเรื่อง comment ไม่ตรงโค้ด — ตรวจแล้ว: **ไม่ใช่ปมจริง ผมพลาดเอง**

ขอแก้ไขสิ่งที่ผมพูดไว้ในรายงานก่อนหน้า — ไปอ่าน `makeMatchDecision()` ตัวเต็มแล้วเจอ comment ที่บรรทัด 1204 บอกชัดว่า:

> `[V6.0.030] Refactored — extracted rules to 10b_MatchDecision.gs ... Now: dispatcher that tries each rule in order`

**Comment เดิมถูกต้อง 100%** — คำว่า "decision rules แยกไป 10b" หมายถึงตัว **rule evaluators** (`evaluateRule1_...` ถึง `evaluateRule8_...`) ซึ่งอยู่ใน 10b จริงตามที่ comment บอก ส่วน `makeMatchDecision` (dispatcher) กับกลุ่ม scoring ไม่เคยถูกอ้างว่าย้าย — ผมเข้าใจผิดเองตอนสรุปรอบก่อนโดยไม่ได้เปิดไฟล์ 10b ดูจริง ขอโทษด้วยครับ

**แต่ระหว่างตรวจเจอปัญหาที่แท้จริงกว่านั้น** — เป็น **backward dependency** ที่มีคุณค่ากับ Phase 2 มาก:

- `10b_MatchDecision.gs` (evaluateRule4/5/5b, บรรทัด 150/176/242) **เรียก** `calculateWeightedScore()` ซึ่งนิยามอยู่ใน `10_MatchEngine.gs`
- `10b_MatchDecision.gs` (บรรทัด 189/193/276/280) **เรียก** `getCandidateResolvedCoords_()` ซึ่งก็นิยามอยู่ใน `10_MatchEngine.gs` เช่นกัน

พูดง่ายๆ คือไฟล์ "ย่อย" (10b) ต้องพึ่งฟังก์ชันจากไฟล์ "หลัก" — ทิศทาง dependency กลับด้าน ควรจะเป็น 10b self-contained มากกว่านี้

**ของแถมที่เจอ — dead code จริง:** `matchCalcFullScore_` และ `matchCalcGeoAnchorScore_` (บรรทัด 1377-1421 เดิม) **ไม่มีใครเรียกเลยในทั้งโปรเจกต์** — มี comment ที่บรรทัด 1325 บอกตรงๆ ว่า `calcDynamicWeights_`/`calculateWeightedScore` "Replaces" ฟังก์ชัน 2 ตัวนี้ แต่ของเก่าไม่เคยถูกลบออก

---

# 2) Phase 2 Extraction Plan (เสนอ — ยังไม่ได้ execute)

จัดกลุ่มตาม **หลักฐานการเรียกใช้จริง** ไม่ใช่แค่กะขนาดบรรทัด:

| Cluster | ฟังก์ชัน | ~บรรทัด | ปลายทาง | เหตุผล |
|---|---|---|---|---|
| **A — Scoring & Dispatch** | `makeMatchDecision`, `calcDynamicWeights_`, `calculateWeightedScore`, `getGeoProvince_`, `getCandidateResolvedCoords_` | 239 | **merge เข้า `10b_MatchDecision.gs`** | แก้ backward dependency ที่เจอข้างบน — 10b จะ self-contained เต็มรูปแบบ |
| **A (ลบทิ้ง)** | `matchCalcFullScore_`, `matchCalcGeoAnchorScore_` | 45 | **ลบ (dead code)** | ไม่มีใครเรียกเลย ยืนยันด้วย full-repo grep |
| **B — Row Processing** | `processOneRow`, `executeDecision`, `handleAutoMatch_`, `handleCreateNew_`, `handleReview_`, `breakTieAmongCandidates`, `getDriverHistory_`, `getStreetDistance_`, `parseDistanceStringToMeters_` | 478 | **ไฟล์ใหม่ `10g_MatchRowProcessor.gs`** | ทั้งกลุ่มนี้คือ "ตัดสินใจแล้ว → ทำจริง" ต่อ 1 แถว — `breakTieAmongCandidates` เรียก 3 ฟังก์ชันท้ายกลุ่มภายในตัวมันเอง ยืนยันแล้วว่าไม่มีใครนอกกลุ่มเรียก |
| **C — Auto-Resume Trigger Mgmt** | `installAutoResume_`, `cleanupOrphanAutoResumeTriggers_`, `isPipelineStopRequested_`, `clearPipelineStopSignal_`, `removeAutoResume_` | 234 | **ไฟล์ใหม่ `10h_MatchAutoResume.gs`** | เรื่อง trigger/resume state ล้วนๆ แยกจาก matching logic ได้สะอาด |

**ผลลัพธ์โดยประมาณ:** 1,603 (หลัง Phase 1) − 284 (A) − 478 (B) − 234 (C) ≈ **~607 บรรทัด** เหลือใน `10_MatchEngine.gs` (บวก overhead comment/header จริงคงขึ้นไปแถว 650-700) — **ต่ำกว่าเป้า 800 อย่างสบายๆ** โดยไม่ต้องแตะกลุ่มอื่นเพิ่ม

⚠️ ตัวเลขนี้คำนวณจากระยะห่างบรรทัดต้น-ท้ายฟังก์ชัน (ไม่นับ comment/blank แม่นยำ 100%) — ต้องรัน `wc -l` ยืนยันจริงหลัง extract

**ความเสี่ยงที่ต้องระวังตอน execute:**
- Cluster A เป็นการ **merge เข้าไฟล์ที่มีอยู่แล้ว** (ไม่ใช่สร้างใหม่) — เสี่ยงชนโครงสร้าง/ชื่อ section เดิมของ 10b มากกว่า Phase 1 ที่สร้างไฟล์ใหม่ล้วนๆ ต้องรีวิวละเอียดกว่า
- ทั้ง 3 cluster ยังต้องเช็คเหมือน Phase 1 ว่ามี module-level state ข้ามกลุ่มไหม (แบบ `_ALIAS_ENRICHMENT_CONTEXT`) — จากที่ดูคร่าวๆ ยังไม่เจอตัวแปร module-level อื่นในฟังก์ชันกลุ่มนี้ แต่ต้องยืนยันอีกครั้งตอน execute จริง

ถ้าโอเคกับแผนนี้ บอกได้เลยครับ จะ execute ต่อแบบเดียวกับ Phase 1 (extract verbatim + diff verify + syntax check)

---

# 3) ตอบคำถาม: `_ALIAS_ENRICHMENT_CONTEXT` reset จาก `runMatchEngine()` — ต้องแก้อะไรไหม?

**คำตอบตรงๆ: ไม่จำเป็นต้องแก้ครับ ทำงานถูกต้องตามที่ออกแบบไว้แล้ว**

เหตุผล: GAS V8 runtime concatenate ทุกไฟล์ `.gs` เป็น script เดียวก่อน execute (ตามสมมติฐานที่ระบุไว้ตั้งแต่ต้นงาน audit) และจุดที่ `runMatchEngine()` แตะตัวแปรนี้ทั้ง 3 จุด (early-return ×2 + finally) เป็นการรันตอน **runtime หลังโหลดสคริปต์ครบแล้ว** ไม่ใช่ top-level code ที่รันตอนโหลด — ลำดับไฟล์จึงไม่มีผลอะไรเลย ปลอดภัย 100%

**สิ่งที่แนะนำ (optional, ไม่บังคับ):** ตอนตรวจ Code Review รอบแรกผมชมแพทเทิร์น `releaseScriptLock_(lock)` ใน `14_Utils.gs` ว่าเป็นตัวอย่างที่ดี (wrap raw operation เป็น named helper function แทนแตะตัวแปร/object ตรงๆ หลายจุด) — ถ้าอยากคง convention เดียวกัน แนะนำเปลี่ยนจาก:

```js
_ALIAS_ENRICHMENT_CONTEXT = null;   // ปรากฏ 3 จุดใน runMatchEngine()
```

เป็นเรียกฟังก์ชัน helper ตัวเดียว (เพิ่มใน `10f_MatchAliasEnrichment.gs`):

```js
function resetAliasEnrichmentContext_() {
  _ALIAS_ENRICHMENT_CONTEXT = null;
}
```

**ประโยชน์:** ถ้าวันหนึ่งโครงสร้าง context เปลี่ยน (เช่น ต้อง log ทุกครั้งที่ reset, หรือเปลี่ยนจาก `null` เป็น object ว่าง) แก้จุดเดียวพอ ไม่ต้องไล่หา 3 จุดข้ามไฟล์ — เป็นการลด coupling surface ไม่ใช่แก้บั๊ก เพราะไม่มีบั๊กอยู่แล้ว

อยากให้ผมทำ wrapper เล็กๆ นี้ให้เลยไหมครับ หรือจะรอทำพร้อม Phase 2 ทีเดียว?

Now I have exact boundaries. Let me extract the two blocks precisely with sed.Good, clean extraction. Now let's assemble the new file with a proper header, matching the codebase's documentation convention.Let's verify syntax and diff against the source to make sure the extraction is byte-exact (no accidental logic drift).Now let's rebuild the assembled file with the corrected block.Now let's verify syntax cleanly:Now let's verify byte-exact fidelity against the original source (no accidental edits during extraction).ทำให้แล้วครับ — สรุปสิ่งที่ทำและสิ่งที่ต้องบอกตรงๆ:

**สิ่งที่ทำ:**
สร้าง `10f_MatchAliasEnrichment.gs` ด้วยการ **extract แบบ verbatim** (ใช้ `diff` เทียบต้นฉบับบรรทัดต่อบรรทัดจนมั่นใจว่าไม่มี logic เปลี่ยนแม้แต่ตัวเดียว) — ย้าย `autoEnrichAliasesFromFactBatch_` + กลุ่มฟังก์ชัน alias-writing ทั้ง 11 ตัวตามที่คุณระบุ พร้อม syntax check ผ่าน

**สิ่งที่เจอระหว่างทำและต้องแจ้ง:**
1. **cross-file state coupling** — `_ALIAS_ENRICHMENT_CONTEXT` (module-level `let`) ถูก reset จาก `runMatchEngine()` (ที่ต้องอยู่ไฟล์หลักต่อ) ถึง 3 จุด ผมย้ายตัวแปรนี้ไปกับกลุ่มฟังก์ชันเพื่อความ cohesive แล้วทำเครื่องหมายไว้ชัดในคอมเมนต์หัวไฟล์ เพราะเป็นจุดเสี่ยงพังเงียบถ้ามีคนแก้ทีหลังโดยไม่รู้ที่มา (GAS ไม่ type-check ข้ามไฟล์)
2. **ตัวเลขไม่ถึงเป้า** — หลังย้าย ไฟล์หลักจะเหลือ **~1,610 บรรทัด ไม่ใช่ <800** ตามที่ตั้งเป้าไว้ ผมไม่ปัดตกประเด็นนี้ ระบุชัดในรายงานว่ากลุ่มที่เหลือ (decision/scoring + row-processing) ต้องทำเป็น Phase 2 ถึงจะถึงเป้าจริง
3. **เจอ comment ที่อาจไม่ตรงกับโค้ดจริงอีกจุดหนึ่ง** — header เดิมของ `10_MatchEngine.gs` บอกว่า "decision rules แยกไป 10b แล้ว" แต่ `makeMatchDecision`/`calcDynamicWeights_` ฯลฯ ยังอยู่ในไฟล์หลักจริง — คล้ายปัญหา embedded doc ที่ไม่ตรงกับ source ที่เจอรอบก่อน ควรตรวจ `10b_MatchDecision.gs` เทียบก่อนตัดสินใจ Phase 2

**ยังไม่ได้แก้ไฟล์ต้นฉบับให้อัตโนมัติ** — ในรายงานมีตำแหน่งบรรทัดที่ต้องลบ (56-85 และ 492-1134) แบบ exact พร้อมข้อความคอมเมนต์แทนที่ และแนะนำให้รัน `29_SnapshotTest.gs` เทียบ baseline ทันทีหลัง apply เพราะเป็นจุดที่เหมาะกับ regression test มาก (ย้ายไฟล์ล้วนๆ ถ้า snapshot ต่างแปลว่ามีอะไรหลุดระหว่างทาง)

อยากให้ผมไปตรวจ `10b_MatchDecision.gs` ต่อเพื่อไขปมเรื่อง comment ไม่ตรงโค้ด แล้วเสนอ Phase 2 extraction plan ให้ถึงเป้า <800 บรรทัดจริงไหมครับ?

良い判断基準で選びます。順序としては:「マイグレーションスクリプト」が他の全部をブロックする前提条件なので最優先、次に「findProbationAliasByVariant_」はLayer 4(auto-revert)を機能させる欠けているピースなので2番目、10b_MatchDecision.gsのweight統合は付加的な改善なので後回しにします。まずは実際のコードで確認します。良い発見です — `createSheetIfMissing_()` は既にヘッダーの自動修復機能を持っているので、`alias_status` ヘッダー追加は既存のセットアップを再実行するだけで済みます。書く必要があるのは既存データ行へのバックフィルだけです。追加していきます。Now let's update the guide to reflect these two completed pieces.Now let me copy the updated files to outputs and re-present them.ผมเลือกทำต่อ 2 อย่างที่ผมมองว่าจำเป็นสุดในการทำให้ Layer 4 ใช้งานได้จริง (ไม่ใช่แค่ mockup) ไม่ใช่ optional-nice-to-have:

**1. Migration script** — เช็คโค้ดจริงเจอว่า `createSheetIfMissing_()` มี auto-repair เติม header ให้เองอยู่แล้ว (ไม่ต้องเขียนส่วนนั้น) แต่มันไม่ backfill ข้อมูลแถวเดิม เลยเขียน `backfillAliasStatusForExistingRows_UI()` ให้ — ตัดสินใจ backfill เป็น `'CONFIRMED'` ไม่ใช่ `'PROBATION'` เพราะ alias เดิมนับพันตัวผ่านการใช้งานจริงมาแล้ว ไม่ควรโดนลด weight ย้อนหลังโดยไม่มีเหตุผล

**2. `findProbationAliasByVariant_()`** — เติมให้ครบ เพราะไม่งั้น auto-revert (ส่วนสำคัญของ Layer 4 ที่กันไม่ให้ alias ผิดๆ ยังทำงานต่อหลังถูก reject) จะเป็นแค่โครงเปล่าที่เรียกใช้ไม่ได้จริง ใช้ exact-match (ไม่ fuzzy) ตามที่บันทึกเหตุผลไว้ในโค้ด

ตอนนี้ทั้งไฟล์ `.gs` (840 บรรทัด, syntax check ผ่านแล้ว) และ guide อัปเดตครบแล้ว — **ระบบ 5-layer ตอนนี้สมบูรณ์ทุก layer ในระดับที่รันได้จริง** เหลือแค่จุดที่ผมจงใจไม่แตะเพราะยังไม่ได้อ่านไฟล์ให้ละเอียดพอ (`10b_MatchDecision.gs` weight integration) กับจุดที่ต้องให้คุณตัดสินใจเชิงธุรกิจ (จะเปิด shadow-mode นานแค่ไหนก่อนตัดของเดิม)

ขั้นต่อไปที่แนะนำจริงๆ คือ**รัน patch 1-3 แล้วลอง execute ใน Apps Script editor จริง** เพื่อจับ runtime error ที่ static analysis จับไม่ได้ (เช่น permission, quota) — ถ้าเจอ error ตรงไหน ส่งมาได้เลยครับ จะช่วยไล่ต่อ

Now I understand the exact hook point (`resolveAndPersistMerge_` in `10e_MatchResolvePersist.gs` calling `createGlobalAlias(..., 'HUMAN', ...)` directly with `active_flag=true` immediately) and the existing infrastructure I can reuse (`levenshteinDistance`, `logAuditTrail`, `PIPELINE_ALERT_CONFIG`/`sendPipelineAlert_`, `SYS_NEGATIVE_SAMPLES`). Let me design the schema additions first, then write the module.Good — that confirms the exact one-line pattern needed for setup. Now let me check the `markAsNegativeSample_` call site for the Layer 4 auto-revert hook.Let me verify one assumption I made in the patch guide before finalizing — whether canonical-name getters actually exist.I caught an inaccuracy in my own draft — let me fix it before handing this over (Zero-Hallucination check: I claimed getter functions exist that don't).Confirmed. Now I'll add the two correctly-implemented helper functions to the safeguard module and fix the guide text.Now fix the guide's incorrect claim:Now let's sanity-check the JS syntax of the deliverable before presenting it.Syntax is clean. Let me copy the deliverables into the outputs directory and present them.เขียนเสร็จแล้วครับ — 2 ไฟล์:

**`21b_AliasSafeguard.gs`** (โมดูลใหม่ ยังไม่กระทบไฟล์เดิม): implement ครบ 5 layer

1. **Structural Validation** — `validateAliasStructure_()` ใช้ `levenshteinDistance()` ที่มีอยู่แล้วใน `14_Utils.gs` คำนวณ similarity ratio, floor = 0.5 (ตั้งค่าได้ผ่าน `SAFEGUARD_CONFIG`)
2. **Repetition Consensus** — `recordAliasProposal_()` เก็บ `confirmation_days_json` แบบนับเฉพาะวันไม่ซ้ำ กัน spam-approve วันเดียว
3. **Conflict Detection** — `detectAliasConflict_()` เช็คทั้ง one-to-many (เทียบกับ `loadGlobalAliasesMap_()` เดิม) และ reverse collision (dry-run ผ่าน `resolvePerson`/`resolvePlace`)
4. **Probation Period** — `promoteStagedAliases_()` เขียนผ่าน `createGlobalAlias()` เดิม (Single Writer ไม่เปลี่ยน) แล้วตั้ง `alias_status='PROBATION'`, มี `graduateProbationAliases_()` และ `autoRevertAliasOnRejection_()` ครบ
5. **Circuit Breaker** — `checkAliasCircuitBreaker_()` นับ daily count ผ่าน `PropertiesService` + เรียก `sendPipelineAlert_()` เดิมจาก `24_PipelineManager.gs`

**`SAFEGUARD_INTEGRATION_GUIDE.md`** — diff ที่ต้อง apply เองใน 6 ไฟล์เดิม (`01_Config.gs`, `02_Schema.gs`, `03_SetupSheets.gs`, `10e_MatchResolvePersist.gs`, `24_PipelineManager.gs`, `12_ReviewService.gs`) พร้อม rollout checklist แบบ shadow-mode ก่อนตัดของเดิมออกจริง

**สิ่งที่ต้องบอกตรงๆ ตาม Zero-Hallucination Policy:** ระหว่างเขียนผมเกือบอ้างว่ามี canonical-name getter สำเร็จรูปอยู่แล้วใน `06_PersonService.gs`/`07_PlaceService.gs` — ตรวจโค้ดจริงแล้วไม่มี เลยเขียน `getPersonCanonicalName_()`/`getPlaceCanonicalName_()` เพิ่มเองในไฟล์ใหม่แทน (wrap รอบ `loadAllPersons_()`/`loadAllPlaces_()` ที่มีอยู่จริง) แก้ทั้งในโค้ดและ guide เรียบร้อยแล้ว

**สิ่งที่ยังไม่ได้ทำ / ต้องตัดสินใจร่วมกับคุณต่อ:**
- Migration script สำหรับเพิ่ม column `alias_status` ใน M_ALIAS ที่มีข้อมูลอยู่จริงแล้ว
- `findProbationAliasByVariant_()` (helper ที่ patch 6 อ้างถึง) — ยังไม่เขียนเพราะต้องตกลงก่อนว่า exact-match หรือ fuzzy-match
- Exact patch สำหรับ `10b_MatchDecision.gs` (ใช้ `getAliasWeightMultiplier_()`) — ยังไม่ได้อ่านทั้งไฟล์ 353 บรรทัดในรอบนี้

อยากให้ผมทำต่อจุดไหนก่อนครับ — migration script, `findProbationAliasByVariant_()`, หรือไปอ่าน `10b_MatchDecision.gs` เพื่อ patch จุดคำนวณ score จริง?
